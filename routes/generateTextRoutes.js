import express from 'express';
// import { generateText } from '../controllers/generateTextController.js'; // Create this controller

const router = express.Router();

// router.post('/', generateText);

export default router;
